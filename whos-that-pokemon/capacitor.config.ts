import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.victorbriaux.pokemon',
  appName: 'whos-that-pokemon',
  webDir: 'dist/whos-that-pokemon/browser'
};

export default config;
